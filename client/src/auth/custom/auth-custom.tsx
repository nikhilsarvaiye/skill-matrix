export const CustomAuthenticatedTemplate = ({ children }: any) => {
    return <div>{children}</div>;
};

export const CustomUnauthenticatedTemplate = ({ children }: any) => {
    return <div>{children}</div>;
};
