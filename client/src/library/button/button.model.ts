export enum ButtonType {
    Default = 'default',
    Primary = 'primary',
    Ghost = 'ghost',
    Dashed = 'dashed',
    Link = 'link',
    Text = 'text',
}

export enum SubmitAction {
    Default = 'default',
    Close = 'close',
    New = 'new',
    Add = 'add',
    Back = 'back',
}

export enum ButtonHTMLType {
    Button = 'button',
    Submit = 'submit',
    Reset = 'reset',
}

export enum ButtonSize {
    Small = 'small',
    Middle = 'middle',
    Large = 'large',
}
