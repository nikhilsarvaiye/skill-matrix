export enum FormSectionLayoutType {
    Horizontal = 'horizontal',
    Vertical = 'vertical',
}

export enum FormSectionTheme {
    Default = 'default',
    White = 'white',
}

export enum FormSectionAlignment {
    Left = 'left',
    Right = 'right',
    Center = 'center',
}
