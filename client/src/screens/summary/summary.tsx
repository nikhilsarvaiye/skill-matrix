import { Screen } from '@screens/screen';
import { Summary } from '@components/summary';

export const SummaryScreen = () => {
    return (
        <Screen>
            <Summary />
        </Screen>
    );
};
